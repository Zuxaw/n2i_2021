export interface People {
  id: string;
  first_name?: string;
  last_name?: string;
  birthdate?: string;
  birthplace?: string;
  physical_description?: string;
  missing_since?: string;
  found_since?: string;
}
